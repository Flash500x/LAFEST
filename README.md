# LAFEST

A static website project for **LAFEST**, a festival/event showcase built with HTML and CSS.  
This repository contains the full frontend source for the LAFEST web pages along with supporting assets and folder structure.

## 🧱 Project Structure

The repository consists of:

- **index.html** – The main landing page of the website.
- **style.css** – Global styles used across the site.
- **Subfolders** containing images, fonts, and individual pages or sections:
  - `DanceOMania/`
  - `Events/`
  - `LA_ttire/`
  - `La Persona/`
  - `Legacy/`
  - `aboutus/`
  - `block n tangles/`
  - `enquesta/`
  - `fonts/`
  - `harmony/`
  - `images/`
  - `lebate/`
  - `rules/`

These folders likely correspond to different sections, events, or pages of the festival site.

## 🚀 Preview

You can open `index.html` in any modern browser to preview the website locally:

1. Clone the repository
   ```bash
   git clone https://github.com/Flash500x/LAFEST.git
